fastapi~=0.128.0
uvicorn[standard]~=0.39.0

numpy~=1.26
# Pillow >=11.3 for NATIVE AVIF decode (wheels bundle libavif on manylinux +
# Windows). HEIC/HEIF is handled by the pillow-heif plugin below.
pillow~=11.3
# HEIC/HEIF decode: libheif-bundled Pillow plugin. Wheels ship libheif for
# manylinux (Render) and Windows (dev) — no system libheif needed. AVIF is
# native to Pillow >=11.3, so pillow-heif 1.x no longer registers an AVIF opener.
pillow-heif~=1.4
scikit-image~=0.24

opencv-python-headless~=4.11.0
mediapipe==0.10.14

reportlab~=4.4
python-multipart~=0.0.20
python-dotenv~=1.0

# Order optimizer
httpx~=0.27
stripe~=10.0
playwright~=1.44

# Local-dev TLS: use the OS trust store so outbound HTTPS works behind a
# corporate TLS-intercepting proxy. Injected in Main.py only when RENDER is unset.
truststore~=0.10

# Database (Phase 9 — Neon Postgres migration)
asyncpg~=0.29
alembic~=1.13
# Sync Postgres driver for alembic (SQLAlchemy default for postgresql:// URLs).
# asyncpg is async-only and not usable by alembic's standard engine flow.
# psycopg2-binary ships pre-built wheels — no compiler needed on Windows.
psycopg2-binary~=2.9
